# Video to Audio Extraction 🎬➡️🎵

This is a Python project to extract audio from video files and save it in audio format.

## ✅ Features
- Extract audio from video (`.mp4`)
- Save output as audio file (example: `.wav`)
- Simple Python script

## 📂 Project Files
- `audio.py` → main Python script
- `audio.mp4` → sample input video
- `11.wav` → extracted audio output

## 🛠 Requirements
Install Python libraries:

```bash
pip install moviepy
